# 🚀 Quick Start

This repository demonstrates how to perform REST API automation using pure Python, PyTest, Requests module, etc.

The goal is to keep the setup simple and focus on core API testing concepts using only the built-in modules and lightweight packages.

# 📌 Features

- Simple API test structure using plain Python
- Uses the requests library for HTTP calls
- JSON validation using built-in json module
- No custom framework or folder structure required

# 🚀 How to Run Tests

You can run tests simply using:
    ```bash
    python3 APICalls.py
    ```